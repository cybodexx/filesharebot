# Non-secret defaults — safe to commit.
# Secrets (API keys, tokens, passwords) must be set as real environment variables.
APP_NAME=vidshare.in
PORT=5000
BIND_ADDRESS=0.0.0.0
MAX_FILE_SIZE_MB=2000
LINK_EXPIRY_DAYS=10
DELETE_AFTER_DOWNLOAD=False
HAS_SSL=True
